import React from 'react';
import Button from '@mui/material/Button';

const Submit = () => {
  return (
    <Button
      type="submit"
      variant="contained"
    >
      Войти
    </Button>
  );
};

export default Submit;